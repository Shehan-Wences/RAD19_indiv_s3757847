<% provide(:title, 'Create a Category') %>
<h1>Create Course</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@category, url: catcreate_path) do |f| %>
        <%= render 'shared/categoryerror_messages' %>

      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>



      <%= f.submit "Create the category", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>